// Placeholder for Khaleb, you can build here. Feel free to change anything here.
// Backend endpoints ready at:
//   POST http://localhost:3001/api/auth/login  ->  body: { email, password }
//   On sucess: call login(data.user, data.token) from useAuth(), then navigate("/whiteboard")

function LoginPage() {
  return (
    <div style={{ maxWidth: 400, margin: "80px auto", padding: "2rem" }}>
      <h2>Login</h2>
      <p>Login form goes here.</p>
    </div>
  );
}

export default LoginPage;